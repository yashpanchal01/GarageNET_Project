# Copy to .env and fill in. GarageNET targets PostgreSQL.
# Flip USE_SQLITE=1 to run on a zero-setup SQLite file during early dev.
USE_SQLITE=0

DB_NAME=garagenet
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432
